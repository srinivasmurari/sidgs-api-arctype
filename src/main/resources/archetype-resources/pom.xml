<project>
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.sidgs.apigee</groupId>
    <artifactId>${projectName}-${teamName}-${artifactId}-parent</artifactId>
    <packaging>pom</packaging>
    <version>1.0.0-SNAPSHOT</version>
    <name>${projectName}-${teamName}-${artifactId}-parent</name>

    <parent>
        <groupId>com.sidgs.api.commons</groupId>
        <artifactId>sidgs-parent-pom</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <properties>
        <teamName>${teamName}</teamName>
    </properties>

	<modules>
		<module>edge</module>
	</modules>
</project>